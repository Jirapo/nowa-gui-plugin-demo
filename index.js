const pretask = require('./pretask');
const promts = require('./promts');
const tasks = require('./tasks');

// 插件名字
const name = {
  zh: '插件名',
  en: 'PluginName'
};


module.exports = {
  name,
  promts,
  tasks,
  pretask,
};
